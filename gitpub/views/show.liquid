<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

</head>
<body>
    <style>
        body {
            color:blue;
            background-color: bisque;
            text-align: center;
        }
        h1 {
            font-size: 60px;
        
        }

        img{
            width: 50%;
            height: 50%;
        }
    </style>
    <h1>At foo bar</h1>
    <h2>{{drink.name}}</h2>
     <img src="{{drinks.image}}.png">
     <h3>${{drinks.price}}</h3>
     <a href="/drinks">Back</a>
    
    
    <ul>
        {% for drink in allDrinks %}
        <li>style="text-transform:capitalize">
            <a href="/drinks/{{forloop.index0}}">{{drinks.name}}</a>

        </li>
        {%endfor%}
    </ul>
</body>
</html>