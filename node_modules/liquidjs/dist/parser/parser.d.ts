import { Liquid } from 'liquidjs';
import { ParseStream } from 'liquidjs/dist/parser/parse-stream';
import { Tag } from 'liquidjs/dist/template/tag/tag';
import { Output } from 'liquidjs/dist/template/output';
import { HTML } from 'liquidjs/dist/template/html';
import { Template } from 'liquidjs/dist/template/template';
import { TopLevelToken } from 'liquidjs/dist/tokens/toplevel-token';
import { LookupType } from 'liquidjs/dist/fs/loader';
export default class Parser {
    parseFile: (file: string, sync?: boolean, type?: LookupType, currentFile?: string) => Generator<unknown, Template[], Template[] | string>;
    private liquid;
    private fs;
    private cache;
    private loader;
    constructor(liquid: Liquid);
    parse(html: string, filepath?: string): Template[];
    parseTokens(tokens: TopLevelToken[]): Template[];
    parseToken(token: TopLevelToken, remainTokens: TopLevelToken[]): Tag | Output | HTML;
    parseStream(tokens: TopLevelToken[]): ParseStream<TopLevelToken>;
    private _parseFileCached;
    private _parseFile;
}
