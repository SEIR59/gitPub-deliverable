import { Liquid } from 'liquidjs';
import { TemplateImpl } from 'liquidjs/dist/template/template-impl';
import { Emitter, Context, TagToken, Template, TopLevelToken } from 'liquidjs/dist/types';
import { HashValue } from 'liquidjs/dist/template/tag/hash';
export declare class Tag extends TemplateImpl<TagToken> implements Template {
    name: string;
    private impl;
    constructor(token: TagToken, tokens: TopLevelToken[], liquid: Liquid);
    render(ctx: Context, emitter: Emitter): Generator<unknown, unknown, HashValue | unknown>;
}
