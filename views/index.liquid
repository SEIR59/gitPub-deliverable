<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
    body{
        background-color: #03013D;
        color: white;
    }
    
    </style>
    <title>Jordan's GitPub</title>
</head>
<body>
    <h1>Welcome to GitPub</h1>
    <h2>Drinks</h2>
    <ul>
    {% for drink in allDrinks%}
        <li><a href =/menu/drink/{{forloop.index0}}>{{drink.name | capitalize}}</a></li>
    {% endfor%}
    </ul> 
    <h2>Food</h2>
    <ul>
    {% for food in allFoods%}
        <li><a href =/menu/food/{{forloop.index0}}>{{food.name | capitalize}}</a></li>
    {% endfor %}
    </ul>
</body>
</html>

