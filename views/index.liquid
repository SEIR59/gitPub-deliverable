<!DOCTYPE html>
<html lang="en">
  <head>
    <style>
      body {
        background-color: lightblue;
        font-size: medium;
        color: white;
      }
      a {
        color: gold;
        font-size: x-large;
        font-weight: 800;
      }
    </style>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>Welcome to gitPub</h1>

    <ul>
      {% for drink in drinks %}
      <li>
        <a href="/drinks/{{ forloop.index0 }}">{{ drink.name | capitalize }}</a>
      </li>
      {% endfor %}
    </ul>

    <br />
    <br />

    <ul>
      {% for item in food %}
      <li>
        <a href="/food/{{ forloop.index0 }}">{{ item.name | capitalize }}</a>
      </li>
      {% endfor %}
    </ul>
  </body>
</html>
