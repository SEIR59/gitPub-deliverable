<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <style type="text/css">
      body {
        color: blanchedalmond;
        background-color: steelblue;
      }
    </style>
    <h1>At foo bar</h1>
    <h2>
      <ul>
        {% for drinks in pub %}
        <li>
          <a href="/drinks/{{ forloop.index0 }}">{{ drinks.name }}</a>
        </li>
        {% endfor %}
      </ul>
    </h2>
    <img src={{ bar.image | append: '.png'}}alt="images" width="200" height="200"/>
    <h3>Price: ${{ drink.price }}</h3>
    <a href="/drinks">Back</a>
  </body>
</html>
